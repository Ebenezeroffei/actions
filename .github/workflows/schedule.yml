name: Scheduled Workflows

on:
  schedule:
    - cron: "*/2 * * * *"

jobs:
  schedule:
    runs-on: ubuntu-latest
    steps:
      - name: Show the current time
        run: echo "This is the current time ${date}"
